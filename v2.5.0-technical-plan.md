# Технический план обновления MpeiApp до v2.5.0

## Статус, границы и ветки

Статус: **этапы 0–4 и 5.1 приняты; код 5.2 реализован и ожидает ручной
Android-приёмки; этапы 6–7 не начаты**.

Исходная точка — выпущенная v2.4.0. Этот документ переносит все явно отложенные
части предыдущего плана и добавляет согласованные задачи v2.5.0. Он не является
разрешением на изменение исходников, зависимостей, секретов, сервера или
удалённого Git-репозитория.

Рабочая модель Git для этой версии:

| Назначение | Ветка |
| --- | --- |
| Повседневная разработка и CI | dev |
| Проверенный релизный срез | v2 |
| Историческая стабильная ветка без новых изменений | main |

До релиза изменения разрабатываются в dev. После приёмки ровно проверенный
релизный коммит переносится в v2. main не является целью обычных коммитов или
автоматического merge.

## Состав v2.5.0

### Новые задачи

1. Отдельное состояние успешной аутентификации без найденного Личного кабинета
   студента вместо ошибочного LOGIN_FAIL.
2. Переработанная справка на экране входа с разворачиваемыми блоками.
3. Быстрый показ навигации после минимально необходимой загрузки БАРС.
4. Анимированные этапы на LoadingScreen для всех сценариев загрузки.
5. Отдельный идентификатор Yandex Ads для каждого inline/sticky-размещения,
   новые баннеры гостевого расписания, зачётной книжки и стипендий.
6. Полная интеграция DragoNet: proxy, проверка подписки, demo-доступ, карточка
   в «Прочем» и инструкции по импорту demo-подписки.

### Перенесённое из плана v2.4.0

1. Автономный защищённый DragoNet HTTPS proxy и его deployment-документация.
2. Мобильный сервис проверки подписки, политика grace/revoke и связь с
   рекламой/лояльностью.
3. Минимальные критические unit/component/integration-тесты без массовых
   snapshot-тестов.
4. GitHub Actions для качества TypeScript, Metro, Android debug APK и proxy.
5. Безопасное materialization Google Services JSON в CI только при
   необходимости debug-сборки; без release keystore, VPN Token и production
   advertising IDs.

## Зафиксированные решения

- Время, относящееся к БАРС и DragoNet, вычисляется в зоне Europe/Moscow.
- Полные персональные ссылки DragoNet, Token панели, raw IP, Device String,
  TLS-ключи и production ad IDs не попадают в Git, логи или аналитические
  события. Исключение по согласованному UX-сценарию: `demoSubURL` и ISO-время
  его получения сохраняются только локально в MMKV до конца трёхсуточного demo;
  запись удаляется и не показывается, когда до истечения остаётся менее часа.
- Для копирования demo-ссылки используется уже установленный
  @react-native-clipboard/clipboard; новую зависимость для этого не добавлять.
- Ссылки на регистрацию БАРС, App Store и APK не показываются пользователю
  текстом: вместо них отображаются кнопки, открывающие URL через Linking.
- Android реализуется и принимается первым. iOS проходит отдельную сборку и
  ручную приёмку после Android gate.
- Демонстрационные Yandex IDs остаются в debug. В release не настроенный
  placement получает demo ID, а не пустой запрос.

## Этап 0. Базовая проверка и общая архитектура

1. Перед началом реализации зафиксировать чистое состояние dev, версии Node 24,
   Yarn Classic и результаты typecheck, Jest, Metro и Android debug build.
2. Не создавать package-lock.json и не переносить секреты из игнорируемого
   Secrets.ts в репозиторий.
3. Ввести небольшие изолированные модули для бизнес-правил, а не расширять
   BARS.ts неявными флагами:

   - src/Login/StudentAccountState.ts — месяц, маскирование пароля и
     классификация состояния отсутствующего кабинета;
   - src/Loading/LoadingProgressService.ts — сессии и этапы загрузки;
   - src/Vpn/* — parser ссылки, transport, persistence, policy и UI state;
   - src/Ads/AdPlacements.ts либо эквивалентный типизированный реестр
     placement-ключей.

4. Все новые сетевые операции получают timeout, schema validation и обработку
   отмены/unmount. Фоновая загрузка от предыдущего аккаунта не должна менять
   Redux/MMKV после logout или нового входа.

### Результат выполнения этапа 0 (03.09.2026)

- Перед изменениями `dev` была чистой, Node — 24.13.0, Yarn Classic — 1.22.22.
- Успешно выполнены `yarn typecheck`, запуск Metro с проверкой `/status` и
  `android/gradlew.bat assembleDebug` (локально заданные JDK 24 и Android SDK;
  задача завершилась `BUILD SUCCESSFUL`).
- Jest пока не запускает сами тесты: preset не преобразует ESM-экспорт
  `@react-navigation/native`. Это исходная проблема конфигурации, не связанная
  с изолированными модулями этапа 0; её исправление отложено до этапа 6.
- Добавлены не подключённые к UI/сети фундаментальные модули Login, Loading,
  Vpn и Ads, перечисленные выше. Они не меняют существующее поведение.
- `package-lock.json` не создан, секреты не затронуты.

## Этап 1. Состояние STUDENTS_NOT_FOUND и экран входа

### Целевое состояние

В LoginState добавить отдельное состояние STUDENTS_NOT_FOUND. Это состояние
аутентифицированного пользователя с ещё не подготовленным Личным кабинетом, а
не BARSError и не вариант LOGIN_FAIL.

~~~text
логин/пароль → NEED_2FA → код 2ФА принят → получение студента
                                            ├─ студент получен → ONLINE
                                            └─ ошибка до студента
                                               ├─ январь/февраль/август/сентябрь
                                               │  → STUDENTS_NOT_FOUND
                                               └─ иной месяц → обычная ошибка входа
~~~

### Точная граница классификации

1. В BARS.ts явно отслеживать фазу одной первичной попытки: ожидание 2ФА,
   код принят, основные данные студента получены. Не определять сценарий по
   тексту исключения вроде split of undefined.
2. Кандидатом на STUDENTS_NOT_FOUND является **любая** ошибка в первичной
   online-цепочке после того, как сервер принял код второго фактора, и до
   установки mCurrentData.student/доступных семестров. В частности это
   включает ошибку redirect, multi-account route, ответ главной страницы,
   parser student info и неожиданный ответ БАРС.
3. Неверный/запрещённый код 2ФА, ошибка отправки кода, отсутствие сети до
   принятия кода, неверный логин/пароль, offline fallback, session restore
   при Init(..., false), test mode и ошибки после получения student data не
   классифицируются как STUDENTS_NOT_FOUND.
4. Проверять только месяцы 1, 2, 8 и 9 в Europe/Moscow. Функция получения
   месяца должна принимать дату/clock как параметр в тестах, а не зависеть
   напрямую от текущего момента.
5. Login2FA не должен преждевременно превращать post-2FA исключение в
   LOGIN_FAIL. Для ожидаемого сценария он возвращает типизированный outcome,
   который верхний auth flow переводит в LoginState.
6. Когда студент уже получен, любой последующий сбой остаётся обычной
   частичной ошибкой загрузки данных; состояние входа не откатывается.

### Данные и повторная попытка

1. При переходе в STUDENTS_NOT_FOUND сохранить введённые credentials в
   STORAGE_KEYS.CREDENTIALS и в памяти BARSAPI. Удалить stale student cache и
   данные БАРС, относящиеся к прежнему пользователю, чтобы чужой офлайн-срез не
   оказался доступен после новой попытки. Общие настройки оформления, consent и
   отдельное хранилище лояльности не затрагивать.
2. Вынести целевой метод повторной попытки, например
   RetryStudentAccountCheck(): он берёт сохранённые credentials, показывает
   AUTHENTICATED_LOADING_DATA, снова проходит normal login/2FA flow и только
   после получения студента выходит в обычную загрузку.
3. «Выйти из аккаунта» использует тот же осознанный logout path, что и
   Drawer: удаляет credentials, temporary 2FA code и данные БАРС, сбрасывает
   LoginState в NOT_LOGGED_IN. Никакой пароль не логируется.
4. При logout/new-login увеличить session generation. Все незавершённые
   загрузки и post-login задачи сверяют этот generation перед dispatch,
   записью в MMKV и переходом состояния.

### UI

1. App.tsx и LoginScreenWrapper отображают STUDENTS_NOT_FOUND как часть
   экрана входа, без полей ввода логина/пароля и без Alert «Ошибка входа».
2. Показать сохранённый логин и маскированный пароль. Для пароля длиной от 3
   символов используется первый символ + нужное число звёздочек + последний;
   для длины 1–2 не раскрывать больше, чем требует правило первого/последнего
   символа. Значение не делать копируемым и не передавать в accessibility
   label целиком.
3. Основной текст:

   > Вход в аккаунт БАРС выполнен, но не найдено ни одного Личного Кабинета
   > студента. Если вы недавно поступили в МЭИ, то это нормально — вуз ещё не
   > успел всё подготовить, просто пробуйте снова через несколько дней. Если
   > проблема сохранится дольше пары недель — свяжитесь с разработчиком.

4. Под текстом разместить две явные кнопки: «Выйти из аккаунта» и
   «Проверить снова». На повторной проверке кнопка блокируется до завершения
   попытки; экран загрузки получает свой этап.
5. Проверить как интерактивный вход, так и автологин с сохранённым временным
   кодом 2ФА: они не должны обходить новую границу состояния.

### Приёмка этапа

- В разрешённых месяцах все post-2FA ошибки до student data показывают
  специальный экран и сохраняют credentials.
- В остальных месяцах те же ошибки остаются обычной ошибкой входа.
- При существующем student data состояние STUDENTS_NOT_FOUND недостижимо.
- «Проверить снова» корректно проходит 2ФА при необходимости; logout возвращает
  стандартную форму входа без старых данных.

### Результат выполнения этапа 1 (03.09.2026)

- `STUDENTS_NOT_FOUND` стал отдельным `LoginState` и типизированным outcome, а
  не `BARSError`. Он возникает только для первичной online-попытки после
  успешного принятия кода 2ФА и до сохранения данных студента, в разрешённые
  месяцы Europe/Moscow.
- Добавлены generation-токены входа/загрузки: устаревшая попытка, ответ 2ФА
  или post-login задача не переводят приложение обратно в старое состояние.
- При специальном состоянии credentials сохраняются, а кэш и Redux-данные
  БАРС очищаются. Обычный logout из Drawer использует тот же путь и не удаляет
  тему, consent или состояние лояльности.
- Экран входа показывает сохранённый логин, невыделяемую маску пароля,
  объяснение и действия «Проверить снова» / «Выйти из аккаунта».
- `yarn typecheck` выполнен успешно. Android-сборка по договорённости не
  запускалась; автоматические тесты остаются задачей этапа 6.

## Этап 2. Новая справка LoginScreen

1. Оставить вверху Help короткое приветствие: для работы с личными данными
   нужны логин и пароль БАРС, данные сохраняются только на устройстве, а карта
   и поиск расписания доступны без входа.
2. Остальную информацию разделить на доступные разворачиваемые блоки с
   понятным состоянием expanded/collapsed. Подойдёт существующий
   react-native-paper List.Accordion либо небольшой собственный компонент;
   новую UI-библиотеку не добавлять.
3. Минимальный набор блоков:

   - «Где взять логин и пароль БАРС?»;
   - «Как работает двухфакторная аутентификация?»;
   - «Что доступно без входа?»;
   - «Конфиденциальность и реклама»;
   - «Нужна помощь?».

4. В блоке «Где взять логин и пароль БАРС?» использовать следующий
   отредактированный по длине, но не по смыслу текст:

   - сначала проверить личный кабинет абитуриента на сайте приёмной комиссии,
     но подчеркнуть, что его логин/пароль не подходят для БАРС — это разные
     системы;
   - проверить основную почту и «Спам»: там может быть приветственное
     сообщение с логином и идентификационным номером;
   - при наличии этих данных предложить кнопку «Зарегистрироваться в БАРС»,
     открывающую https://mpei.ru/Pages/registration.aspx, без печати URL в UI;
   - честно предупредить: обещание активации через 20 минут неверно, ожидание
     занимает минимум сутки и иногда до недели;
   - если этот путь не подходит и других сведений нет, ждать информацию от
     старосты/представителей вуза и периодически проверять личный кабинет
     приёмной комиссии; доступ может быть выдан после начала учёбы, в том числе
     не на первой учебной неделе, а способы различаются между группами.

5. Кнопки внешних ресурсов используют Linking.canOpenURL/openURL с
   пользовательским нейтральным сообщением при невозможности открыть
   приложение/браузер. URL не попадает в видимый текст.
6. Проверить прокрутку, контраст обеих тем, screen reader, кнопку «Назад» и
   сохранение раскрытого состояния только в пределах текущего открытия Help.

### Результат выполнения этапа 2 (03.09.2026)

- Help на LoginScreen содержит короткое вводное пояснение и пять доступных
  разворачиваемых разделов. Открытым может быть один раздел; это состояние
  существует только пока открыта справка.
- Добавлен полный сценарий получения учётных данных БАРС с кнопкой
  «Зарегистрироваться в БАРС». URL не попадает в текст интерфейса, а перед
  открытием проверяется через `Linking.canOpenURL`.
- Тексты о 2ФА, гостевом доступе, конфиденциальности/рекламе и поддержке
  перенесены в отдельные разделы. Кнопка «Назад» и прокрутка сохранены.
- `yarn typecheck` выполнен успешно. Android-сборка по договорённости не
  запускалась; визуальная приёмка обеих тем остаётся за ручной проверкой.

## Этап 3. Быстрая загрузка БАРС и прогресс на LoadingScreen

### Общий сервис этапов

1. Создать LoadingProgressService с изолированными сессиями. Каждая сессия
   имеет идентификатор, текущий этап и один предыдущий завершённый этап:

   ~~~ts
   type LoadingStageStatus = 'success' | 'failed'
   type LoadingProgress = {
     current?: {label: string}
     previous?: {label: string; status: LoadingStageStatus}
   }
   ~~~

2. API сервиса должен поддерживать start/advance/complete/fail/clear и
   игнорировать событие неактуальной сессии. Экран подписывается только на
   переданный progressKey; закрытие экрана очищает подписку, а не чужую
   сессию.
3. LoadingScreen получает progressKey и безопасный fallback label. Поэтому
   ни один существующий вызов LoadingScreen не остаётся полностью без текста.
4. Под Lottie-анимацией отрисовать:

   - текущий этап белым цветом, с плавным появлением снизу;
   - предыдущий этап выше, полупрозрачным;
   - к предыдущему добавлять «успешно» зелёным или «не удалось» красным.

   Для перехода использовать Animated/Reanimated, уже доступный проекту, без
   нового пакета. Быстрая смена нескольких этапов не должна ломать layout или
   показывать старую сессию.

### Покрытие всех существующих загрузок

| Сценарий | Минимальные этапы |
| --- | --- |
| Инициализация приложения | «Подготовка приложения...» |
| Ввод логина/пароля | «Проверка логина и пароля...», затем «Запрос к БАРС...» |
| Ввод и проверка 2ФА | «Проверка кода подтверждения...», затем «Получение основных данных студента...» |
| Основная online-загрузка | «Получение основных учебных данных...», затем «Подготовка основных разделов...»; внутри параллельно запрашиваются текущая неделя, оценки и долги |
| Фоновые разделы БАРС | конкретный этап при активном экране либо fallback «Загрузка раздела...» |
| Карта | «Загрузка карты...»; при fallback показать предыдущий результат корректно |
| Поиск расписания | «Поиск расписания...», «Загрузка занятий...» |
| Камера QR | «Подготовка камеры...» |

Для Books, Tasks, Reports, Orders, Questionnaires, RecordBook, Stipends,
SkippedClasses, Marks и обычного расписания необходимо передать хотя бы
fallback/конкретный progressKey. Ошибка конкретной операции получает красный
статус до перехода к обычному FetchFailed или Alert.

### Разделение core и background загрузки

1. Переписать LoadOnlineDataInternal из последовательной цепочки в два
   управляемых набора задач.
2. До перехода в LOGGED_IN запускать и ждать terminal state только для:

   - уже полученных основных данных студента и списка семестров;
   - текущих оценок;
   - долгов (запрос оценок прошлого семестра, если он существует);
   - текущей недели.

   Независимые core-запросы запускать параллельно через Promise.allSettled;
   зависимость «долги → доступный прошлый семестр» сохраняется. Сбой core
   раздела переводит его Redux slice в FAILED, но не держит приложение на
   бесконечном LoadingScreen после успешной аутентификации.

3. Сразу после завершения core-набора перейти в LOGGED_IN и показать
   навигацию/оценки. Это является границей UX, а не ожиданием всех данных.
4. После этой границы запускать ограниченную фоновую очередь (не
   неконтролируемый Promise.all, чтобы не перегружать БАРС) для:

   - личного расписания — **строго первым** заданием очереди: оно может
     загружаться долго и не должно задерживать вход или показ оценок;
   - фильтра доступных семестров;
   - почты;
   - пропусков;
   - зачётной книжки;
   - заданий;
   - отчётов;
   - стипендий;
   - приказов;
   - книг;
   - анкет;
   - DragoNet revalidation.

   Очередь фиксирует результат каждого раздела и не создаёт unhandled
   rejection. Пользователь может открыть раздел во время его загрузки и
   увидеть корректный LoadingScreen/FetchFailed.

5. Убрать общий модальный Alert о фоновой ошибке как побочный эффект долгой
   загрузки. Core-сбои отображаются у соответствующего раздела, а фоновый
   сбой не блокирует навигацию; безопасный диагностический лог остаётся.
6. Изменить контракт RegisterPostOnlineDataTask: задачи DragoNet запускаются
   после core gate и не блокируют LOGGED_IN. Это намеренное уточнение старого
   плана ради приоритета скорости интерфейса.
7. Сохранить single-flight защиту. Новая попытка login/logout отменяет или
   делает неактуальными обе очереди предыдущей сессии.

### Повторная загрузка отдельных разделов

1. Для каждого загружаемого Redux-раздела БАРС (оценки, личное расписание,
   пропуски, зачётная книжка, задания, отчёты, стипендии, приказы, книги и
   анкеты) добавить единый целевой retry API. Он переводит только этот раздел
   в загрузку и не перезапускает приложение, а также объединяется с уже
   работающей задачей фоновой очереди для того же раздела.
2. В состоянии `FAILED` экран раздела показывает кнопку «Попробовать снова».
   Если отображается последняя сохранённая копия (`OFFLINE`), эта кнопка
   располагается внутри предупреждения об устаревших данных. Пользователь
   может перейти в другие разделы, пока повторная попытка выполняется.
3. Ошибка проверки почты отображается в Drawer как «не удалось обновить» с
   небольшой кнопкой повтора со стрелкой справа. Она повторяет только
   проверку почты, не затрагивая остальные данные.
4. Retry, logout и новый login используют generation/session guard: поздний
   ответ старого аккаунта не меняет Redux/MMKV и не подменяет данные нового.

### Приёмка этапа

- При искусственно медленных Mail/Books/Reports навигация появляется сразу
  после core-набора, а не ждёт второстепенные запросы.
- При искусственно медленном личном расписании навигация и оценки появляются
  до его завершения; расписание запускается первым в фоновой очереди.
- Экран последовательно показывает текущий и предыдущий этапы; успешная и
  неуспешная стадии имеют соответственно зелёный и красный статус.
- На карте, в поиске расписания, 2ФА и QR нет пустого loader.
- Нельзя увидеть события загрузки прошлого пользователя после logout/new-login.
- Каждый неудачно загруженный раздел и почту можно повторить точечно без
  перезапуска приложения; повтор не создаёт дубликат фонового запроса.

### Результат выполнения этапа 3 (04.09.2026)

- `LoadingProgressService` подключён к анимированному `LoadingScreen`:
  текущая стадия появляется снизу, а завершённая остаётся выше со статусом
  «успешно»/«не удалось». Все существующие loader-сценарии получили предметный
  fallback-текст; авторизация, 2ФА, online core и поиск расписания передают
  конкретные стадии.
- Core gate теперь параллельно ждёт только текущую неделю, оценки и расчёт
  долгов. После него открывается навигация, а ограниченная фоновая очередь
  запускает личное расписание первым, затем остальные разделы и post-online
  задачи.
- Добавлен generation-safe single-flight API повтора разделов. Кнопка
  «Попробовать снова» доступна при `FAILED` и внутри предупреждений `OFFLINE`
  для оценок, расписания, пропусков, зачётной книжки, заданий, отчётов,
  стипендий, приказов, книг и анкет. В Drawer ошибка почты получает компактную
  кнопку повтора со стрелкой.
- Выполнен `yarn typecheck` — успешно. Android-сборка по договорённости не
  запускалась; поведение очереди, повторов и анимаций ожидает ручной приёмки.

## Этап 4. Идентификаторы Yandex Ads и новые размещения

### Типизированный реестр

1. Заменить выбор ID только по формату на выбор по placement-ключу. Формат
   сохраняется в типе placement и нужен для demo fallback/создания BannerView,
   но production ID выбирается по точному месту показа.
2. В Secrets.example.ts и игнорируемом Secrets.ts использовать одинаковый
   набор названий у android и ios:

| Placement | Ключ в YANDEX_AD_UNIT_IDS | Судьба существующего ID |
| --- | --- | --- |
| Inline после оценок главного БАРС | inlineBannerMarks | перенести сюда прежний inline ID |
| Inline внизу Drawer | inlineBannerDrawer | новый отдельный ID |
| Inline внизу сегодняшнего дня расписания | inlineBannerScheduleToday | новый отдельный ID |
| Inline под поиском гостевого расписания без запроса | inlineBannerGuestScheduleEmpty | новый отдельный ID |
| Sticky LoadingScreen | stickyBannerLoading | перенести сюда прежний sticky ID |
| Sticky «Пропуски» | stickyBannerSkippedClasses | новый отдельный ID |
| Sticky деталей оценок | stickyBannerDetailedMarks | новый отдельный ID |
| Sticky «Зачётная книжка» | stickyBannerRecordBook | новый отдельный ID |
| Sticky «Стипендии» | stickyBannerStipends | новый отдельный ID |
| Rewarded | rewarded | оставить текущую схему, если размещение одно |

3. До release вручную перенести реальные прежние inline/sticky значения в
   обозначенные строки. Это сохраняет уже используемые рекламные ID для одного
   placement каждого формата, но даёт независимую статистику всем остальным.
4. Обновить тип OptionalSecrets, getYandexAdUnitId, createAdRequest,
   InlineBannerAd и StickyBannerAd так, чтобы компонент требовал placement и
   невозможно было случайно запросить generic inlineBanner/stickyBanner.

### Размещения и layout

1. Передать явные placement в уже существующие баннеры:
   главный экран оценок, Drawer, сегодняшнее расписание, LoadingScreen,
   пропуски и детали оценок.
2. В GuestScheduleScreen добавить inlineBannerGuestScheduleEmpty строго под
   ScheduleSearchPanel. Баннер виден только пока нормализованный поисковый
   запрос пуст; при вводе текста и после перехода к результатам скрывается.
   Состояние query при необходимости поднять из ScheduleSearchPanel в родителя.
3. В RecordBookScreen добавить StickyBannerSlot с
   stickyBannerRecordBook, в StipendsScreen — с stickyBannerStipends. Показывать
   их в loaded/offline состоянии и резервировать в FlatList paddingBottom
   высоту реально загруженного баннера, safe area и базовый нижний отступ.
4. Расширить AD_PLACEMENTS, StickyAdPlacement и initial reserved heights.
   Сохранить правило: в одном видимом экране активен не более один sticky;
   unmount/focus change освобождает claim и reserved height.
5. Рекламные gates не меняются: ad-free purchase или ACTIVE/GRACE DragoNet
   скрывают любой banner/rewarded placement без flash первого кадра.

### Приёмка этапа

- На Android и iOS ключи конфигурации совпадают по именам.
- Запрос каждого placement получает свой выбранный ID; debug использует
  соответствующий demo ID.
- Новый inline появляется только на пустом гостевом поиске.
- Sticky в зачётной книжке и стипендиях не перекрывает последнюю карточку,
  экранную навигацию или safe area, исчезает при ошибке/выключенной рекламе.

### Результат выполнения этапа 4 (05.09.2026)

- Реестр `AdPlacements` теперь является типизированным источником placement-
  ключей. `getYandexAdUnitId` и `createAdRequest` принимают только конкретный
  placement; формат применяется исключительно для выбора соответствующего
  demo ID. Generic `inlineBanner`/`stickyBanner` больше нельзя передать в
  banner-компонент.
- `Secrets.example.ts` и локальный игнорируемый `Secrets.ts` приведены к
  одинаковым ключам Android/iOS. Прежние production ID перенесены в
  `inlineBannerMarks` и `stickyBannerLoading`; остальные placement оставлены
  независимыми для назначения отдельных ID перед release.
- Все существующие inline/sticky-размещения получили явные ключи. Добавлен
  inline под пустым поиском гостевого расписания, а в «Зачётной книжке» и
  «Стипендиях» — sticky-слоты только для состояний `LOADED`/`OFFLINE` с
  резервом фактической высоты баннера, safe area и нижнего отступа.
- Sticky-claim освобождается при уходе экрана из фокуса; LoadingScreen
  использует собственный placement. Рекламные gates не менялись.
- Выполнен `yarn typecheck` — успешно. Android-сборка по договорённости не
  запускалась; размещения и отступы ожидают ручной приёмки в APK.

## Этап 5. DragoNet

Этап разделён на две самостоятельные части: **5.1 `vpn-subscription-proxy`**
и **5.2 интеграция в приложении**. Сначала развёртывается и принимается proxy;
мобильный transport, UI и публичные константы подключаются только после этого.

### 5.1. `vpn-subscription-proxy` — развёрнуто и принято (10.09.2026)

#### 5.1.1. Целевая схема и разграничение секретов

~~~text
MpeiApp
  ├─ VpnSubscriptionService
  │    ├─ verify: нормализованное имя клиента
  │    └─ demo: ссылка в result sheet и локальном блоке до истечения demo
  └─ POST verify/demo + Mpei-App-Req-Id
                  ↓
DragoNet HTTPS proxy на VPS
  ├─ verify → VPN panel GET /apiv2/clients
  └─ demo   → VPN panel POST /apiv2/save
~~~

Мобильное приложение хранит нормализованное имя клиента, timestamps и счётчики
policy. После согласованной корректировки UX оно дополнительно хранит только
`demoSubURL` и время выдачи, пока до серверного expiry demo остаётся не менее
часа. URL панели, Token, TLS private key, demo HMAC key, данные клиентов и raw
IP/device остаются исключительно на VPS.

Создать автономный проект vpn-subscription-proxy в корне репозитория, без
превращения React Native проекта в monorepo:

~~~text
vpn-subscription-proxy/
├─ src/
│  ├─ server.ts
│  ├─ startupDiscovery.ts
│  ├─ requestGate.ts
│  ├─ vpnPanelClient.ts
│  ├─ verifySubscription.ts
│  ├─ createDemoAccess.ts
│  ├─ demoCooldownStore.ts
│  └─ schema.ts
├─ test/
├─ package.json
├─ tsconfig.json
├─ .env.example
├─ README.md
└─ deploy/
   ├─ dragonet-subscription-proxy@.service
   ├─ dragonet-subscription-proxy-certificate-reload@.service
   └─ dragonet-subscription-proxy-certificate-reload@.path
~~~

Использовать небольшой TypeScript/Fastify HTTPS-сервис на Node 24 с schema
validation, timeout, rate limit и тестами. SQLite runtime state лежит вне Git,
например в /var/lib/dragonet-subscription-proxy.

#### 5.1.2. Startup discovery и HTTPS listener

Перед открытием публичного listener при **каждом** запуске:

1. Параллельно запросить IPv4 у api.ipify.org, ifconfig.me/ip и icanhazip.com.
   Принять только нормализованный глобальный IPv4 при кворуме не менее 2 из 3;
   иначе fail-closed без публичного listener.
2. В первую очередь проверить пары fullchain.pem + privkey.pem в
   /etc/letsencrypt/live/*/, раскрыв symlink. Проверить читаемость, соответствие
   ключа сертификату, срок и SAN/CN.
3. Подходящим считать только конкретное DNS-имя сертификата, которое через DNS
   точно указывает на IP из голосования. Wildcard без конкретного имени
   недостаточен.
4. Для непривилегированного systemd service использовать LoadCredential от
   той же пары /etc/letsencrypt/live/<certbot-name>/{fullchain.pem,privkey.pem}:
   PID 1 читает её при старте, сервис получает краткоживущий read-only snapshot
   через CREDENTIALS_DIRECTORY и валидирует его тем же способом. Не создавать
   постоянную копию private key.
5. Только если Let’s Encrypt-пары и systemd credential не подошли, проверить ограниченный fallback
   allow-list: /etc/ssl, /etc/nginx/ssl, /opt/*/ssl, /root/{fullchain.pem,
   privkey.pem}, /root/{cert.pem,key.pem} и одноуровневые пары
   /root/.acme.sh. Не сканировать рекурсивно диск или /root.
6. Proxy сам завершает TLS, сверяет Host/SNI и строит demoSubURL. Nginx с TLS
   termination этому контракту не соответствует. При продлении сертификата
   systemd PathChanged unit отслеживает live directory и symlink, запускает
   one-shot restart service и discovery повторяется без Certbot deploy-hook.

#### 5.1.3. Контракт proxy и request gate

~~~text
POST <PROXY_REQUEST_PATH>
Content-Type: application/json
Mpei-App-Req-Id: DragoNet-DD-MM-YYYY-DEVICE_STRING

{ "purpose": "verify", "clientName": "<normalised name>" }
{ "purpose": "demo" }
~~~

1. purpose — strict discriminated union. verify требует валидное
   нормализованное имя; demo не принимает имя. Полная личная ссылка никогда не
   отправляется для verify.
2. До parsing body gate принимает только заголовок с актуальными DD-MM-YYYY в
   Europe/Moscow и непустым остатком DEVICE_STRING без control characters.
3. При отсутствующем, неверном или просроченном заголовке закрыть raw socket
   **без HTTP-ответа**, маршрутизации, JSON parsing, rate limit и логирования.
   Публичных health/statistics endpoint нет; /healthz и /stats доступны только
   loopback.
4. Для валидного запроса применить schema/длину, timeout, rate limit и
   краткий cache по HMAC имени только в verify.
5. Единственные публичные JSON-ответы:

| Сценарий | Ответ |
| --- | --- |
| Найден активный клиент | { "reqStatus": "success", "isClientFound": true, "isClientActive": true } |
| Найден неактивный клиент | { "reqStatus": "success", "isClientFound": true, "isClientActive": false } |
| Клиента нет | { "reqStatus": "success", "isClientFound": false, "isClientActive": false } |
| Demo создано | { "reqStatus": "success", "demoSubURL": "https://…" } |
| Cooldown/rate-limit/upstream/schema error | { "reqStatus": "failed" } |

6. Причины отказа, panel response, Token, TLS-paths, raw IP/device и клиентское
   имя не попадают в ответ или журналы. Gate — только фильтр трафика, не
   криптографическая аутентификация; узкие schema/timeout/rate limit/cooldown
   остаются обязательными.
7. /stats на loopback возвращает in-memory счётчики с последнего запуска:
   успешные verify, успешные demo и запросы, которым proxy вернул
   reqStatus failed, а также uptime в сутках, часах и минутах. Запросы с
   неверным Mpei-App-Req-Id не учитываются: socket закрывается ещё до
   маршрутизации.
8. verify делает GET VPN_PANEL_BASE_URL/apiv2/clients с Token и Accept:
   application/json. Сопоставление — точное client.name === clientName;
   active означает найденный client.enable === true.

#### 5.1.4. Demo и cooldown на сервере

1. После прохода gate demo вызывает S-UI API POST /apiv2/save с object clients,
   action new и сериализованным data, совместимым с текущим Telegram-ботом.
2. Имя: MpeiApp-demo-DD-MM-YYYY-XXXXX, где XXXXX — 5 криптографически
   случайных заглавных букв/цифр. Коллизии имени/password/UUID заранее не
   проверяются.
3. Payload использует настроенные активные demo inbounds, группу Dragon's
   clients, enable true, 2 GiB, expiry через 3 суток, delayStart false,
   autoReset true, resetDays 1, нулевые счётчики и описание
   demo access via MpeiApp. Для trojan, anytls, hysteria2, tuic и vless
   повторить совместимую форму Telegram-бота; password/UUID генерировать
   криптографически и не сохранять.
4. Построить demoSubURL из обнаруженного домена, subscription port/path prefix
   и percent-encoded имени. Вернуть только создавшему запросу, не логировать.
5. SQLite cooldown имеет две независимые HMAC-SHA-256 записи: ip и device.
   Хранятся identityType, HMAC нормализованного значения, issuedAt, expiresAt
   и reservation/issued — без raw IP/device.
6. До создания атомарно зарезервировать обе identity. Совпадение либо IP,
   либо device отклоняет demo без раскрытия причины. При успехе reservation
   фиксируется, при известной ошибке panel освобождается; незавершённая после
   аварии остаётся консервативно блокирующей.
7. Cooldown истекает через один календарный месяц в Europe/Moscow, после чего
   запись удаляется. X-Forwarded-For игнорировать, кроме явного списка
   trusted reverse proxies.

#### 5.1.5. Deployment DragoNet

1. В root-owned EnvironmentFile вне Git держать VPN_PANEL_BASE_URL,
   VPN_PANEL_TOKEN, VPN_DEMO_INBOUNDS, VPN_SUBSCRIPTION_PORT,
   VPN_SUBSCRIPTION_PATH_PREFIX, PROXY_REQUEST_PATH, DEMO_COOLDOWN_HMAC_KEY,
   PROXY_LISTEN_PORT, state directory, cache/rate-limit/timeout и при необходимости
   trusted proxy setting.
2. Во второй части этапа, в Secrets.ts/Secrets.example.ts задать только
   публичные:

   ~~~ts
   export const DRAGONET_PROXY_DOMAIN = ''
   export const DRAGONET_PROXY_PORT = 8443
   export const DRAGONET_PROXY_PATH = ''
   export const DRAGONET_SUBSCRIPTION_PORT = 443
   export const DRAGONET_SUBSCRIPTION_PATH_PREFIX = '/sub'
   export const DRAGONET_TELEGRAM_URL = 'https://t.me/DragonSavA'
   ~~~

   `DRAGONET_PROXY_PORT` хранится отдельной константой, потому что 443 на
   целевом VPS занят. `DRAGONET_SUBSCRIPTION_PORT` и
   `DRAGONET_SUBSCRIPTION_PATH_PREFIX` — публичные части URL, нужные только
   для строгого разбора вставленной ссылки; `DRAGONET_TELEGRAM_URL` задаёт CTA.
   Transport строит только https:// + domain + optional port + path и валидирует
   все части. Перед релизом domain, port, path и subscription URL сверяются с
   результатом startup discovery, PROXY_LISTEN_PORT, PROXY_REQUEST_PATH,
   VPN_SUBSCRIPTION_PORT и VPN_SUBSCRIPTION_PATH_PREFIX.
3. README proxy описывает Debian 11: Node 24, сервисного пользователя,
   закрытый SQLite state directory, production install/build, EnvironmentFile
   root:root 600, systemd LoadCredential для реальной Let’s Encrypt-пары,
   PathChanged restart без Certbot hook, CAP_NET_BIND_SERVICE, systemd
   enable/start, firewall, AF_NETLINK для Fastify interface discovery,
   проверку notAfter/SAN существующего сертификата и
   certbot.timer/renew recovery, loopback healthz/stats,
   безопасную verify-проверку, отсутствие ответа для неверного header,
   update/rollback и безопасные логи.
4. До production deployment проверить, что порт 443 не занят несовместимым
   TLS-terminating Nginx. Не открывать наружу panel API /apiv2/*.

#### Результат 5.1 (10.09.2026)

- Обновлённый systemd unit успешно запущен.
- `verify` корректно прошёл все три проверочных сценария.
- Первый запрос `demo` вернул `reqStatus: "success"` и рабочий `demoSubURL`:
  соответствующий клиент появился в панели, а его трафик корректно учитывался.
- Повторный такой же запрос вернул `{ "reqStatus": "failed" }`; защита от
  абьюза сработала без раскрытия причины отказа.

### 5.2. Интеграция в приложении — реализована, ожидает ручной Android-приёмки

#### 5.2.1. Мобильный сервис и policy

1. Реализовать единый VpnSubscriptionService для verify/demo. Он формирует
   endpoint только из DRAGONET_PROXY_DOMAIN/PORT/PATH, добавляет request ID на
   каждый POST и валидирует ответ до использования.
2. Mpei-App-Req-Id строится как DragoNet-DD-MM-YYYY-DEVICE_STRING с московской
   датой. Неблокирующе получить разрешённый API serial, если он уже доступен;
   иначе штатно использовать название/версию ОС и модель. Не добавлять новые
   опасные runtime permissions ради serial.
3. Создать чистый extractVpnClientName(link): принимает только HTTPS и
   разрешённый origin/path prefix, берёт последний непустой segment, применяет
   decodeURIComponent ровно один раз, не меняет case/[]/@/_ и отклоняет
   чужой host, неверный path, пустое/слишком длинное имя и не-URL.
4. Результаты transport: ACTIVE, INACTIVE_CONFIRMED, TRANSIENT_FAILURE,
   OFFLINE_SKIPPED. reqStatus failed, timeout, socket close и malformed
   response — TRANSIENT_FAILURE, а не доказательство отсутствия подписки.
5. Persistent state:

   ~~~ts
   type VpnVerificationState = {
     clientName?: string
     lastVerifiedAt?: string
     technicalFailureStreak: number
     firstTechnicalFailureAt?: string
     inactiveStreak: number
     firstInactiveAt?: string
     lastEffectiveStatus: 'ACTIVE' | 'GRACE' | 'REVOKED' | 'NONE'
   }
   ~~~

   Отдельно хранить `VpnDemoAccess { demoSubURL, receivedAt }`. Сервер выдаёт
   demo на 3 суток, поэтому при повторном открытии приложения показывать эту
   ссылку до момента, когда до expiry остаётся меньше часа; затем удалять её
   из MMKV. Закрытие result sheet запись не удаляет.

6. Политика:

| Результат | Счётчики | Доступ |
| --- | --- | --- |
| ACTIVE | сбросить обе streak, обновить lastVerifiedAt | ACTIVE |
| INACTIVE_CONFIRMED | увеличить inactive, сбросить technical | grace до порога C |
| TRANSIENT_FAILURE | увеличить technical, сбросить inactive | grace до A/B |
| OFFLINE_SKIPPED | ничего не менять | прежний статус |

   Доступ отзывается только при A: technicalFailureStreak >= 10; B:
   technicalFailureStreak >= 3 и прошло не менее 7 суток с первого такого
   сбоя; C: inactiveStreak >= 3 и прошло не менее 3 суток с первого
   подтверждённого inactive.

7. Автопроверка запускается один раз сразу после core gate успешной
   online-цепочки БАРС и выполняется параллельно с второстепенной фоновой
   загрузкой. Ручная «Проверить снова» использует ту же policy. При отсутствии
   clientName status NONE; offline не меняет counters.
8. ACTIVE/GRACE публикует APP_EVENTS.VPN_ENTITLEMENT_CHANGED. Реклама и
   rewarded выключены; TokenBadge показывает ∞, реальные токены и покупки
   сохраняются. После REVOKED возвращаются только неприобретённые ограничения;
   купленное ad-free остаётся.

#### 5.2.2. Карточка DragoNet и demo-инструкции

1. Создать DragoNetPromoCard на SettingsScreen («Прочее») перед обычными
   ссылками поддержки. Использовать имеющийся промо-арт как ImageBackground
   крупной карточки с тёмным градиентом/backdrop, не как маленькую иконку.
2. Карточка содержит заголовок DragoNet, краткую пользу (YouTube, TikTok,
   Discord, нейросети, игры, от 80 ₽/месяц, российские приложения без
   отключения VPN, нестандартные протоколы), Telegram CTA «Подключить DragoNet»
   и кнопку «Бесплатный демо-доступ».
3. Demo POST запускается только явным нажатием; повторное нажатие блокируется
   до terminal result. Для failed вывести нейтральное сообщение без указания,
   сработал ли cooldown по IP или device.
4. В result sheet и восстановленном блоке demo-ссылка показана отдельной интерактивной строкой. Всё
   поле и расположенная рядом иконка MaterialIcons content-copy копируют её в
   clipboard по нажатию; после успеха показать короткое «Ссылка скопирована».
   У кнопки есть accessibilityLabel «Скопировать ссылку подписки». После
   закрытия sheet ссылка остаётся только в локальном MMKV вместе со временем
   выдачи и показывается при перезапуске, пока до expiry не меньше часа; она
   не логируется и не попадает в Git или аналитику. Основной путь — явное
   копирование и инструкции ниже.
5. После строки ссылки отрисовать только инструкции текущей Platform.OS.

   **iOS**

   1. Кнопка «Открыть в App Store» открывает страницу INCY:
      https://apps.apple.com/ru/app/incy/id6756943388. URL не показывается
      текстом в интерфейсе.
   2. «Запустите INCY, скопируйте и вставьте полученную ссылку подписки
      DragoNet».
   3. «Протестируйте появившиеся конфигурации и дождитесь результатов».
   4. «Выберите одну из рабочих конфигураций и подключитесь к ней — готово!»
   5. «Если у вас уже установлен Happ и в нём привычнее, попробуйте настроить
      подписку аналогично: она совместима с большинством iOS-клиентов».
   6. «Часть конфигураций у вас может не работать - это нормально: они
      рассчитаны на другие приложения/платформы».

   **Android**

   1. Кнопка «Загрузить apk» открывает Owenclave APK:
      https://github.com/owenewans/owenclave/releases/download/v0.17.58/Owenclave-0.17.58-arm64-v8a.apk.
      URL не показывается текстом в интерфейсе.
   2. «Установите приложение, при необходимости разрешив установку из этого
      источника, и запустите его».
   3. «Откройте раздел Groups в нижней панели: он находится справа от
      исходного раздела».
   4. «Находясь именно в Groups, нажмите плюс в правом верхнем углу».
   5. «Предварительно скопируйте ссылку подписки DragoNet. В меню выберите
      Add subscription from URL — ссылка вставится автоматически; нажмите
      Add & Download. После закрытия окна выберите появившуюся группу, если
      она не выбралась автоматически».
   6. «Вернитесь в Configuration — самый левый пункт нижней панели».
   7. «Нажмите кнопку со спидометром сверху и дождитесь результатов
      тестирования».
   8. «Выберите одну из рабочих конфигураций».
   9. «Нажмите кнопку включения в крайнем правом пункте нижней панели, чтобы
      подключиться — готово!»
   10. «Если вам привычнее другое приложение, попробуйте настроить его
       аналогично, но совместимость не гарантирована».
   11. «Часть конфигураций у вас может не работать - это нормально: они
       рассчитаны на другие приложения/платформы».

   После обоих вариантов всегда выводить: «Инструкции для других платформ есть в Telegram-боте».

6. Для внешних кнопок проверять Linking.canOpenURL и показывать понятную
   ошибку, если URL не открывается. Не обещать, что Android APK совместим с
   архитектурами, отличными от arm64-v8a.
7. Ниже demo-блока реализовать состояния персональной подписки: нет ссылки,
   проверяется, ACTIVE, GRACE, INACTIVE/REVOKED, temporary error/offline;
   действия «Проверить», «Проверить снова», «Сменить ссылку» и «Отключить на
   этом устройстве». Полное имя/ссылка не выводится без необходимости. Этот
   раздел изначально свёрнут и раскрывается по нажатию; инструкции по
   подключению также изначально свёрнуты.

#### Результат кода 5.2 (10.09.2026)

- Добавлены transport, request ID, service, MMKV persistence и policy
  DragoNet; фоновая revalidation теперь стартует после core gate, не ожидая
  второстепенные разделы БАРС.
- В «Прочем» добавлена крупная карточка с промо-артом, demo result sheet,
  сохраняемым demo-блоком, свёрнутыми инструкциями и свёрнутой проверкой
  подписки.
- Android APK Owenclave обновлён до v0.17.58, а обязательная оговорка о
  конфигурациях добавлена перед примечанием о других платформах.
- Успешно выполнены `yarn typecheck` и изолированный
  `vpn-subscription-proxy: npm run typecheck`.
- Android-сборка намеренно не запускалась по договорённости; ручная проверка
  функционирования остаётся следующим шагом.

#### Корректировки 5.2 (11.09.2026)

- Для внешних HTTPS-ссылок добавлен Android package-visibility intent в
  `AndroidManifest.xml`. На Android ложный отрицательный результат
  `Linking.canOpenURL` больше не блокирует фактический `Linking.openURL`;
  ошибка показывается только если открытие действительно не удалось.
- Фон карточки заменён на `assets/images/DragoNet/DragoNet.webp`.
- Перед свёрнутой проверкой подписки добавлен краткий призыв: проверка
  отключает рекламу и открывает полный доступ к возможностям MpeiApp.
- Текстовый перечень сервисов заменён непрерывной медленной лентой одинаковых
  webp-логотипов из `AppIconsForPromo`. Пары Copilot/Gemini и Xbox/Nintendo
  сохранены рядом; после ленты размещён текст «Стабильный доступ к любым
  приложениям и сайтам…».
- Кнопка «Подключить DragoNet» использует тот же значок FontAwesome `telegram`,
  что и «ЛС разработчика в Telegram».
- Корректировки первоначальной итерации проверены `yarn typecheck`.

#### Дополнительные корректировки 5.2 (11.09.2026)

- Для каждого логотипа в промо-ленте задан отдельный размер контейнера по
  фактической непрозрачной области webp: визуальный размер YouTube,
  Instagram, TikTok, NTE и остальных логотипов выровнен и увеличен. В конец
  бесшовного цикла добавлен `anitokyotv.webp`.
- В верхний правый угол карточки добавлен двухстрочный лозунг «Чтобы был не
  "Чебурнет" — подключайте DragoNet!». Он использует меньший контрастный
  шрифт, находится поверх фона и не меняет высоту заголовка.
- Когда `lastEffectiveStatus === 'ACTIVE'`, карточка показывает текст
  благодарности, кнопку «Перейти к боту» и оставляет свёрнутую проверку;
  demo-кнопка и призыв к проверке скрыты. После снятия entitlement карточка
  автоматически возвращается к исходному наполнению.
- Кнопка запроса demo не показывается при сохранённой доступной demo-ссылке.
  Alert недоступности объясняет ограничение: во избежание злоупотреблений
  новый demo-запрос доступен лишь раз в месяц.
- Успешно выполнены `yarn typecheck` и `:app:assembleDebug`. Debug APK
  установлен и проверен в запущенном Pixel_6 x86_64 через Metro: лента,
  фон, лозунг и обычное состояние карточки отображаются корректно.

## Этап 6. Тесты и GitHub Actions

### Автоматические тесты

Не добавлять массовые snapshot-тесты. Новые тесты покрывают правила, в которых
регрессия меняет вход, данные, рекламу, деньги или доступ.

| Область | Обязательные сценарии |
| --- | --- |
| STUDENTS_NOT_FOUND | каждый допустимый месяц, любой другой месяц, post-2FA граница, неверный код, уже полученный student, credentials, logout/retry, маска пароля |
| Loading progress | success/fail переход, быстрая смена этапов, изоляция двух сессий, cleanup/unmount, fallback label |
| Core/background BARS | навигация после core, второстепенные задачи не блокируют, terminal FAILED, stale generation не dispatch-ит |
| Login Help | accordion, кнопка регистрации без видимого URL, обработка недоступного external URL |
| Ad placements | точное сопоставление ключ/ID, demo fallback, все inline/sticky props, скрытие при ad-free/VPN, padding и один sticky |
| Guest schedule | баннер только при пустом query; до submit нет fetch/Redux personal schedule |
| VPN URL parser | percent encoding, []/@, неверные host/path/protocol, пустой segment, не-URL |
| VPN policy | ACTIVE reset, transient, inactive 1–3/72 часа, transient 3/7 дней, transient 10, offline без изменения counters |
| Mobile transport | domain/path/port, московская дата, device fallback, mapping response, demoSubURL сохраняется только с временем выдачи |
| DragoNet result sheet | копирование по строке и иконке, iOS/Android тексты и кнопки, восстановление demo после перезапуска, скрытие менее чем за час до expiry, свёрнутые инструкции |
| Proxy discovery | IP vote 3/3 и 2/3, нет кворума, приоритет LE, fallback только после него, DNS/IP и cert/key match |
| Proxy gate/verify | неверный header без HTTP-ответа, точные JSON поля, отсутствие Token/name/raw IP/device в response/log |
| Proxy demo/cooldown | payload, имя, отсутствие сохранения password/UUID, IP/device cooldown, календарный месяц, concurrency не более одного demo |
| Регрессии v2.4 | token economy, unlock/VPN override, Android icon confirmation и базовый App render с моками native SDK |

Добавить scripts typecheck: tsc --noEmit и test:ci: jest --ci --runInBand.
Добавить lint только после подтверждения стабильного non-watch запуска с
совместимой версией ESLint. Proxy имеет собственные install/typecheck/test
scripts и изолированную временную SQLite-базу.

Нативные flows (launcher aliases, реальные Yandex размеры/logcat, карта,
камера, App Store/APK handler) остаются ручной Android/iOS-приёмкой: Jest не
заменяет PackageManager и SDK.

### GitHub Actions

Создать .github/workflows/ci.yml:

~~~yaml
on:
  push:
    branches: [dev, v2]
  pull_request:
    branches: [dev, v2]
~~~

Jobs:

1. mobile-quality — checkout, Node 24, Yarn Classic cache,
   yarn install --frozen-lockfile, typecheck, test:ci и lint, когда готов.
2. metro-smoke — чистый Yarn install, background Metro с timeout/polling
   health endpoint, вывод логов при ошибке и гарантированная остановка.
3. android-debug-apk — Node 24, подтверждённый JDK (24 при возможности,
   fallback 17), debug assemble, опциональный artifact APK. Материализовать
   google-services.json из GitHub Secret только если debug build действительно
   этого требует.
4. vpn-proxy-quality — отдельная воспроизводимая установка в
   vpn-subscription-proxy, typecheck/test с mock panel, DNS/IP/TLS и temporary
   SQLite; без запросов к реальному VPS, IP-сервисам, сертификатам или панели.

В mobile CI не передавать VPN_PANEL_TOKEN, panel URL, proxy .env, release
keystore или production advertising IDs. Production proxy развёртывается
отдельным systemd workflow на VPS, а не из GitHub Actions мобильного проекта.

## Этап 7. Release gate v2.5.0

1. После feature freeze выполнить на чистом окружении Yarn install,
   typecheck, test:ci, Metro smoke и Android assembleDebug.
2. Android acceptance на Pixel_7a:

   - вход с логином/паролем, интерактивный и сохранённый 2ФА;
   - STUDENTS_NOT_FOUND с контролируемой датой/mock responses, retry/logout;
   - core-first загрузка и переход в навигацию при медленных фоновых разделах;
   - все статусы LoadingScreen;
   - Help, регистрационная кнопка и обе темы;
   - каждая реклама с demo IDs, guest inline, record-book/stipends sticky,
     отсутствие перекрытия и ad-free/VPN gates;
   - DragoNet staging proxy, demo cooldown, copy, Android Owenclave
     инструкцию, восстановление demo URL после перезапуска и скрытие менее чем
     за час до expiry;
   - карта, QR-камера, guest schedule, drawer, лояльность и icon flow.

3. После принятого Android gate выполнить CocoaPods/Xcode build в macOS/VMware
   и повторить платформенно значимые пункты: Login Help, LoadingScreen,
   safe areas баннеров, iOS INCY-инструкцию/App Store button, DragoNet
   copy, карту и рекламу.
4. Перед выпуском обновить:

   - package.json до 2.5.0;
   - android versionName до 2.5.0 и versionCode до следующего
     неопубликованного значения;
   - iOS MARKETING_VERSION до 2.5.0 и допустимый build number;
   - WhatsNewScreen.tsx только фактически реализованными пунктами.

5. Выполнить финальную проверку: secrets отсутствуют в diff/APK/IPA, proxy
   deployment inputs находятся только вне Git, все CI checks зелёные,
   production ad keys заполнены осознанно, а v2 получает именно принятый
   коммит из dev. main не изменяется.

## Ориентировочно затрагиваемые области

| Область | Файлы/папки |
| --- | --- |
| Авторизация и загрузка | src/API/BARS.ts, src/API/Error/Error.ts, App.tsx, src/Screens/Login/*, src/Screens/LoadingScreen/*, новый src/Loading/* |
| БАРС-экраны | src/Screens/BARS/*, src/Screens/Schedule/*, src/Screens/Map/MapScreen.tsx, QRCodeScanner.tsx |
| Реклама | src/Ads/*, src/config/Secrets.example.ts, игнорируемый Secrets.ts |
| DragoNet mobile | новый src/Vpn/*, SettingsScreen.tsx, AppEvents.ts, Constants.ts, Loyalty/Ads providers |
| DragoNet server | новый vpn-subscription-proxy/* |
| Качество и CI | __tests__/*, jest config/mocks при необходимости, package.json, .github/workflows/ci.yml |
| Релиз | package.json, android/app/build.gradle, iOS project metadata, WhatsNewScreen.tsx |

## Условия готовности

- Вход с отсутствующим Личным кабинетом не маскируется под неверный логин или
  пароль и не теряет credentials.
- Пользователь видит живой, понятный прогресс и получает навигацию после
  student/marks/debts/week/schedule, не после всех второстепенных разделов.
- Все inline/sticky места имеют отдельный конфигурационный ключ и не
  перекрывают контент.
- Demo DragoNet копируется одним нажатием, содержит корректную инструкцию
  именно для текущей платформы и восстанавливается после перезапуска до часа
  перед server-side expiry.
- DragoNet proxy fail-closed при невалидной identity/TLS-конфигурации и не
  раскрывает секреты.
- Unit/integration/component-тесты и CI покрывают критические правила,
  Android и iOS ручные gate пройдены.
- Версия v2.5.0 и WhatsNew отражают только реально завершённый объём.
