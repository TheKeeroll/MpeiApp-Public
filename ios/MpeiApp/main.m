#import <UIKit/UIKit.h>
#import "MpeiApp-Swift.h" // имя проекта + "-Swift.h" (Xcode сам генерирует)

// Точка входа
int main(int argc, char * argv[]) {
  @autoreleasepool {
    return UIApplicationMain(argc, argv, nil, NSStringFromClass([AppDelegate class]));
  }
}